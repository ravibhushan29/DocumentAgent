# Minimal Prometheus config for local dev
scrape_configs:
  - job_name: "docagent-api"
    static_configs:
      - targets: ["host.docker.internal:8000"]
    metrics_path: /metrics
    scrape_interval: 15s
